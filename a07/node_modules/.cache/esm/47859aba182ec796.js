let keypress,Game;_182‍.w("keypress",[["default",["keypress"],function(v){keypress=v}]]);_182‍.w("./engine/game",[["default",["Game"],function(v){Game=v}]]);


keypress(process.stdin);


/**
 * The code in this file is used to run your game in the console. Use it
 * to help develop your game engine.
 *
 */

let game = new Game(4);
_182‍.g.console.log(game.toString());
let board = {
    board: [ 1024, 1024 ,0, 0,
        0, 0, 0, 2,
        0, 0, 0, 0,
        0, 2, 0, 0],
    score: 0,
    won: false,
    over: false
}
//game.loadGame(board);


_182‍.g.console.log(game.toString());
game.onMove(gameState => {
    _182‍.g.console.log(game.toString());
    _182‍.g.console.log(game.getGameState());
    // console.log(game.gameState);
});

game.onWin(gameState => {
    _182‍.g.console.log('You won with a gameState of...', game.getGameState());
});

game.onLose(gameState => {
    _182‍.g.console.log('You lost! :(', game.getGameState())
    console.log(`Your score was ${game.getGameState().score}`);
});

process.stdin.on('keypress', function (ch, key) {
    switch (key.name) {
        case 'right':
            game.move('right');
            break;
        case 'left':
            game.move('left');

            break;
        case 'down':
            game.move('down');

            break;
        case 'up':
            game.move('up');
            break;
    }
    if (key && key.ctrl && key.name == 'c') {
        process.stdin.pause();
    }
});


process.stdin.setRawMode(true);
process.stdin.resume();

